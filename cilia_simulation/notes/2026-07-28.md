

## 実装状況
- `TwoSliderTimeStepper(record_constraint_Fx=True)` で RHS 時に `(t,s,Fx)` を保存
- `compute_two_slider_Q_blakelet(include_constraint_force_in_Q=True)` は保存系列から Q を計算
- 密な `t_eval` での再計算は本番経路では使わない
- **注意**: 実行中の古い exp09 ジョブはこの最適化を含まない。止めて再起動が必要。
